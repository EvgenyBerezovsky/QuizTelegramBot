<UserControl x:Class="QBManager.View.Students"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:QBManager.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="622"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:StudentVM />
    </UserControl.DataContext>

    <Grid Margin="15">

        <Border CornerRadius="20,20,20,20"
                 BorderThickness="0">
            <Border.Background>
                <ImageBrush ImageSource="/Images/student_page.png"
                            Stretch="None" />
            </Border.Background>


        </Border>

        <Grid Margin="20, 90, 20 , 20">
            <!-- Сетка для разделения списка викторин и деталей -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <!-- Список викторин -->
                <ColumnDefinition Width="*"/>
                <!-- Разделитель -->
                <ColumnDefinition Width="300"/>
                <!-- Детали выбранной викторины -->
            </Grid.ColumnDefinitions>

            <!-- Список викторин -->
            <Border Grid.Column="0" 
                    Background="#272B2F" 
 
                    BorderThickness="0">

                <ListBox ItemsSource="{Binding Users, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
                         SelectedItem="{Binding SelectedUser}" 
                         Background="Transparent" 
                         Foreground="#bdbdbe"
                         BorderThickness="0" 
                         ItemContainerStyle="{StaticResource ListBoxItemStyle}" 
                         ScrollViewer.CanContentScroll="False"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">



                    <ListBox.ContextMenu  >
                        <ContextMenu Style="{StaticResource ContextMenuStyle}">

                            <MenuItem Header="Delete info"
                                      Command="{Binding DeleteInfoCommand}"
                                      Foreground="#bdbdbe"
                                      Style="{StaticResource MenuItemStyle}"/>

                        </ContextMenu>
                    </ListBox.ContextMenu>


                    <ListBox.ItemTemplate>
                        <DataTemplate>

                            <Grid>
                                
                              
                                <TextBlock Grid.Column="1"
                                               HorizontalAlignment="Left"
                                               Text="{Binding UserName}" 
                                               FontSize="16" 
                                               Foreground="#bdbdbe" 
                                               VerticalAlignment="Center"
                                               TextWrapping="Wrap"
                                               Style="{StaticResource TextQuiz_Style}"/>
                            </Grid>

                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

            </Border>




            <!-- Детали выбранной викторины -->
            <Border Grid.Column="2" 
                    Background="#272B2F" 

                    BorderThickness="0" >




                <!-- Список вопросов -->

                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                  Height="450"
                                 >
                    <ItemsControl ItemsSource="{Binding Scores}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#272B2F"  
                                            Padding="10" 
                                            Margin="0,5,0,5" 
                                            BorderBrush="#bdbdbe" 
                                            BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Topic}" Foreground="#bdbdbe" Style="{StaticResource TextQuiz_Style}"/>
                                        <TextBlock Text="{Binding Time}" Foreground="#bdbdbe" />
                                        <TextBlock Text="{Binding Result}" Foreground="#bdbdbe" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

            </Border>
        </Grid>
    </Grid>
</UserControl>
